const express=require("express"),path=require("path"),serveStatic=require("serve-static"),history=require("connect-history-api-fallback"),app=express();app.use(history()),app.use(serveStatic(path.join(__dirname,"spa")));const server=app.listen(process.env.PORT||5e3,(function(){var e=server.address().port;console.log("App now running on port",e)}));